#include "my_functions.h"
/*int absolute_neg(int);*/

int absolute(int n)
{
if (n > 0) {
  return(n);
  }
 else if (n == 0) {
   return(0);
  }
 else {
   return(-n);
 }


/*int absolute_neg(int n)
{
  int n_again;
  int power;
  int digit;

  n = -n;
  n_again = n;
  power = 1;
  while (n_again > 9) {
    n_again /= 10;
    power *= 10;
  }
  
  while (n > 0) {
    digit = n / power;
    n = n % power;
    power /= 10;
    print_char(digit + 48);
  }
  return(0);*/
}
    
