#include "my_functions.h"

void print_combination_2(void)
{
  int n;
  int i;
  for (n = 0; n <= 9; n++)
    {
      for (i = n + 1; i <= 9; i++)
	{
	  print_char((n + 48));
	  print_char((i + 48));
	  if (n < 8)
	    {
	      print_char(',');
	      print_char(' ');
	    }
	}
    }
}    
      
